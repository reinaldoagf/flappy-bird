const RANDOM = Math.floor(Math.random() * 300) 
const WINDOW_INNER_WIDTH = window.innerWidth 
const ELEMENT_SIZE = 100
const FLOOR_HEIGHT = 90
const PLAY_AREA_HEIGHT = window.innerHeight - FLOOR_HEIGHT
export { RANDOM, WINDOW_INNER_WIDTH, ELEMENT_SIZE, FLOOR_HEIGHT, PLAY_AREA_HEIGHT }